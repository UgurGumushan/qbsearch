#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"
if ! command -v bun >/dev/null 2>&1; then
    echo "error: Bun is not installed; install Bun and run 'bun run setup'." >&2
    exit 127
fi
exec bun run static-check
