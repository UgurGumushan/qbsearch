qbsearch 0.1.5 adds a dedicated distribution website for the qBittorrent
search-plugin collection.

## Highlights

- Added a responsive Next.js website in `packages/website`.
- Added a searchable, filterable directory for all 61 catalogued engines.
- Added platform installation guidance for macOS, Linux, and Windows.
- Added `/download/latest`, which resolves to the ZIP produced by CI.
- Added `qbsearch-latest.zip` as a stable alias for the latest release archive.

## Install the collection

1. Download `qbsearch-0.1.5.zip` or `qbsearch-latest.zip` below.
2. Extract the archive and quit qBittorrent.
3. Run the installer for your platform:
   - macOS: `./install/macos.sh`
   - Linux: `./install/linux.sh`
   - Windows PowerShell: `./install/windows.ps1`
4. Relaunch qBittorrent and open the Search tab.

The archive contains all standalone plugin engines, matching icons, support
files, and the native installers. No Python, Bun, or repository checkout is
needed on the target machine.

For single-plugin installation, use the website directory or the generated
catalog documentation to open an individual plugin source file.
