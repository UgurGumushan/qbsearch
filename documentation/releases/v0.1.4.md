qbsearch 0.1.4 migrates repository tooling to Bun and ships native
cross-platform installers.

## Highlights

- Added a catalog-driven user installation and plugin discovery workflow.
- Added cross-platform collection installers and release ZIP packaging.
- Replaced the Python collection installer with dependency-free native scripts
  under `install/` and removed the obsolete compatibility wrappers.
- Added generated plugin documentation and maintainer guidance.
- Moved live-test default queries out of the test runner and into the catalog.
- Replaced Make-based maintainer commands with a Bun command surface and Bun
  test runner.
- Migrated live plugin smoke tests and their helper suite to TypeScript/Bun;
  live checks no longer start Python processes.
- Consolidated shared process and catalog tooling.
- Fixed Python 3.9 harness diagnostics and made CI use a policy-compatible
  Bun installation.

## Install the collection

1. Download `qbsearch-0.1.4.zip` below.
2. Extract the archive and quit qBittorrent.
3. Run the installer for your platform:
   - macOS: `./install/macos.sh`
   - Linux: `./install/linux.sh`
   - Windows PowerShell: `./install/windows.ps1`
4. Relaunch qBittorrent and open the Search tab.

The archive contains all standalone plugin engines, matching icons, support
files, and the native installers. No Python, Bun, or repository checkout is
needed on the target machine.

For single-plugin installation, use the generated catalog documentation to
open an individual plugin source file.

**Full Changelog**: https://github.com/UgurGumushan/qbsearch/compare/v0.1.3...v0.1.4
