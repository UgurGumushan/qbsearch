# Live credentials / session tokens — never commit these
*.cookies
all/danishbytes.cookies

# Python / tooling caches
__pycache__/
*.pyc
.ruff_cache/
plugins/*.log
plugins/*.cookie
plugins/*_temp.*

# macOS
.DS_Store
